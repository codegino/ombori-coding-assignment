import './App.css';
import UsersPage from './pages/UsersPage';

function App() {
  // This may be an unnecessary component,
  // but this is the place where we can control our navigation in case of multiple pages.
  return <UsersPage />;
}

export default App;
